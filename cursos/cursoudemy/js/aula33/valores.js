/* 
Primitivos (imutaveis) - string, number, boolean, undefined, null (bigint, symbol)  v

Referencia (mutável) - Arrays, Objetos, Functions
*/

let a = [1, 2, 3];
let b = [...a]; // copia o valor do A e não aponta na memoria